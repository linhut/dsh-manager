# 🏗️ DSH Manager 架构设计文档

## 一、项目定位

DSH Manager 是一个**跨平台桌面应用**（类似 ccswitch），为 DeepSeek Harness (DSH) 提供：
- **一键安装**：无需任何编程知识，点点鼠标就完成 DSH 安装
- **DSH 控制台**：内置浏览器视图，直接管理 DSH Web 界面
- **插件市场**：浏览、搜索、一键安装 GitHub 上的 DSH 插件
- **版本管理**：查看当前版本、检查更新、切换版本
- **配置管理**：查看和编辑 DSH 配置

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     Electron 主进程                           │
│  ┌───────────────┐  ┌────────────────┐  ┌───────────────┐  │
│  │  main.js      │  │ ipc-handlers.js│  │ preload.cjs  │  │
│  │ 窗口管理/菜单  │  │ DSH 操作/插件  │  │  IPC 桥接     │  │
│  │ 生命周期      │  │ 配置/系统      │  │  API 暴露     │  │
│  └───────┬───────┘  └───────┬────────┘  └───────┬───────┘  │
└──────────┼──────────────────┼────────────────────┼──────────┘
           │                  │                    │
           ▼                  ▼                    ▼
┌─────────────────────────────────────────────────────────────┐
│                   核心逻辑层 (Node.js)                         │
│  ┌────────────────────┐  ┌──────────────────────────────┐   │
│  │  @dsh-manager/core │  │  @dsh-manager/marketplace    │   │
│  │  ─ DSH 安装器     │  │  ─ GitHub API 封装           │   │
│  │  ─ 配置管理       │  │  ─ 插件注册表                │   │
│  │  ─ 版本管理       │  │  ─ 插件安装器                │   │
│  │  ─ 工具函数       │  │  ─ 插件管理器                │   │
│  └────────────────────┘  └──────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────┐
│                    渲染进程 (GUI)                              │
│  ┌──────────────┐  ┌────────────────┐  ┌───────────────┐  │
│  │  index.html  │  │  style.css     │  │  app.js       │  │
│  │  页面结构     │  │  暗色主题      │  │  页面渲染     │  │
│  │  自定义标题栏 │  │  响应式布局    │  │  DSH 状态管理 │  │
│  └──────────────┘  └────────────────┘  └───────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 三、核心工作流

### 3.1 首次使用流程

```
用户下载安装包 → 安装 DSH Manager → 打开应用
                                            │
                                            ▼
                              ┌─────────────────────────┐
                              │ 检测到 DSH 未安装        │
                              │ 显示"安装 DSH"按钮       │
                              └──────────┬──────────────┘
                                         │ 用户点击安装
                                         ▼
                              ┌─────────────────────────┐
                              │ 自动安装 DSH             │
                              │ (npm install -g dsh)    │
                              └──────────┬──────────────┘
                                         │ 安装成功
                                         ▼
                              ┌─────────────────────────┐
                              │ 自动切换到 DSH 控制台    │
                              │ 加载 DSH Web 界面       │
                              │ (http://127.0.0.1:3080) │
                              └─────────────────────────┘
```

### 3.2 日常使用流程

```
打开 DSH Manager
        │
        ▼
┌──────────────────────────────┐
│ DSH 控制台（默认页）          │
│ ┌─────────────────────────┐ │
│ │ DSH Web 界面 (webview)  │ │
│ │ 直接在工具中管理 DSH     │ │
│ └─────────────────────────┘ │
└──────────────────────────────┘
        │
        ├──→ 插件管理 → 浏览市场 → 一键安装
        ├──→ 版本管理 → 检查更新 → 升级
        ├──→ 设置 → 查看配置 → 编辑
        └──→ 关于 → 版本信息 → 更新
```

## 四、插件市场机制

### 数据流
```
用户搜索插件 → IPC 请求 → ipc-handlers.js → PluginRegistry.search()
    → GitHubAPI.searchRepositories(topic:dsh-plugin)
    → 格式化结果 → 返回给渲染进程 → 显示在 GUI 中
```

### 安装流程
```
用户点击"安装" → IPC 请求 → ipc-handlers.js
    → PluginInstaller.install(source)
    → 解析来源 (github:/npm:)
    → 执行 dsh plugin --profile add <package>
    → 注册到本地插件列表
    → 返回结果 → 渲染进程显示成功
```

## 五、多平台构建

### GitHub Actions 自动构建

| 触发条件 | 构建平台 | 输出文件 |
|---------|---------|---------|
| 推送 v* 标签 | Windows (windows-latest) | `.exe` (NSIS 安装包) |
| 推送 v* 标签 | macOS (macos-latest) | `.dmg` (磁盘映像) |
| 推送 v* 标签 | Linux (ubuntu-latest) | `.AppImage` / `.deb` |
| 手动触发 workflow_dispatch | 可选任一平台 | 同上 |

## 六、技术选型

| 组件 | 技术 | 选型理由 |
|------|------|---------|
| 桌面框架 | Electron 33 | 成熟的跨平台方案，Web 技术栈 |
| 前端 | 原生 HTML/CSS/JS | 零依赖，包体积小 |
| 自定义标题栏 | frameless + CSS | 统一各平台外观 |
| DSH 集成 | webview 标签 | 内嵌 DSH Web 界面 |
| 核心逻辑 | Node.js ESM | 直接调用 npm 包 |
| 打包 | electron-builder | 支持 NSIS/DMG/AppImage |
| CI/CD | GitHub Actions | 免费、原生集成 |

## 七、目录结构

```
dsh-manager/
├── electron/           # Electron 主进程
│   ├── main.js         # 主进程入口，窗口管理
│   ├── preload.cjs     # 预加载脚本，IPC 桥接
│   └── ipc-handlers.js # IPC 通信处理
├── src/                # 渲染进程 (GUI)
│   ├── index.html      # 主页面
│   └── assets/
│       ├── css/style.css  # 暗色主题样式
│       └── js/
│           ├── app.js      # 应用初始化与页面渲染
│           └── modules/    # 模块化架构
│               ├── state.js        # 全局响应式状态
│               ├── utils.js        # 工具库 + Toast + 模态确认 + 缓存 + 超时管理
│               ├── debug.js        # 调试日志
│               ├── theme.js        # 主题切换
│               ├── dsh-status.js   # DSH/npm/pnpm/依赖状态检测
│               ├── dsh-control.js  # DSH Webview/启动/停止/诊断
│               ├── page-manager.js # 页面生命周期管理（懒加载）
│               ├── constants.js    # 魔法字符串集中管理
│               └── shortcuts.js    # 键盘快捷键
├── packages/           # 核心逻辑
│   ├── core/           # DSH 安装、配置、版本管理
│   └── marketplace/    # 插件市场
├── build/              # 构建资源 (图标等)
├── .github/workflows/  # CI/CD 配置
├── install.ps1         # 一键安装脚本
└── package.json        # Electron 应用配置
```

## 八、未来规划

- [x] 自动检测 DSH 运行状态并启动/停止（dsh-status/dsh-control 模块）
- [x] 插件市场分类和评分（分类/范围/排序筛选）
- [x] 主题切换（亮色/暗色/跟随系统）
- [ ] 多语言支持 (英文/中文)
- [ ] 自动更新（DSH Manager 本体）
- [ ] 系统托盘
- [ ] 将 app.js 剩余渲染函数进一步拆分为 pages/ 页面模块
- [x] IPC 调用统一通信层（错误码 + 超时 + 重试）
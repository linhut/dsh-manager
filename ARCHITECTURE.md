# 🏗️ DSH Manager 架构设计文档

## 一、项目概述

DSH Manager 是 DeepSeek Harness (DSH) 的官方管理工具，旨在解决 DSH 生态中以下核心痛点：

1. **安装门槛高**：普通用户需要通过 npm 安装，且需要了解 Node.js 环境
2. **插件发现难**：DSH 插件分散在 GitHub 各处，用户难以发现
3. **管理不直观**：配置管理、版本管理缺乏友好的工具支持
4. **生态分散**：缺少一个统一的入口来管理 DSH 的完整生命周期

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│                  用户交互层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │  CLI     │  │  TUI     │  │  Web (官网)       │  │
│  │ (命令行) │  │ (终端UI) │  │ (Landing Page)   │  │
│  └────┬─────┘  └────┬─────┘  └────────┬─────────┘  │
└───────┼──────────────┼────────────────┼──────────────┘
        │              │                │
┌───────┴──────────────┴────────────────┴──────────────┐
│                   业务逻辑层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ 安装管理  │  │ 插件市场  │  │  配置管理        │  │
│  │ Installer│  │Marketplace│  │  Config          │  │
│  └────┬─────┘  └────┬─────┘  └────────┬─────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ 版本管理  │  │ 系统诊断  │  │  自我升级        │  │
│  │VersionMgr│  │ Doctor   │  │  SelfUpgrade     │  │
│  └────┬─────┘  └────┬─────┘  └────────┬─────────┘  │
└───────┼──────────────┼────────────────┼──────────────┘
        │              │                │
┌───────┴──────────────┴────────────────┴──────────────┐
│                   基础设施层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ DSH 工具  │  │ GitHub   │  │ npm Registry     │  │
│  │ dsh-utils │  │ API      │  │ 接口             │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
│  ┌──────────┐  ┌──────────┐                          │
│  │ 文件系统  │  │ 网络请求  │                          │
│  │   I/O    │  │  fetch   │                          │
│  └──────────┘  └──────────┘                          │
└──────────────────────────────────────────────────────┘
```

## 三、核心模块设计

### 3.1 Core 核心库 (`@dsh-manager/core`)

**职责**：提供所有与 DSH 安装、配置、版本管理相关的基础能力。

**模块设计**：

```
core/
├── index.js              # 统一导出入口
├── installer.js          # DSHInstall 类
│   ├── install(version?) # 安装 DSH
│   ├── uninstall()       # 卸载 DSH
│   ├── upgrade()         # 升级 DSH
│   ├── switchVersion()   # 版本切换
│   └── getAvailableVersions() # 获取可用版本列表
├── config.js             # DSHConfig 类
│   ├── read()            # 读取配置
│   ├── write()           # 写入配置
│   ├── get(key)          # 获取配置项
│   ├── set(key, value)   # 设置配置项
│   ├── listLLMProviders() # 列出 LLM 提供商
│   └── listAgentPresets() # 列出 Agent Presets
├── dsh-utils.js          # DSHUtils 工具函数
│   ├── isDSHInstalled()  # 检测 DSH 安装
│   ├── getDSHVersion()   # 获取版本
│   ├── getDSHInfo()      # 获取完整信息
│   ├── checkDSHIntegrity() # 检查完整性
│   └── DSH_PATHS         # 路径常量
├── version-manager.js    # DSHVersionManager
│   ├── getInstalledVersions() # 已安装版本
│   ├── checkForUpdate()  # 检查更新
│   └── getLatestVersion() # 获取最新版本
└── errors.js             # 错误类型定义
```

### 3.2 Marketplace 插件市场 (`@dsh-manager/marketplace`)

**职责**：实现插件发现、安装、管理的完整流程。

**设计要点**：

1. **插件发现机制**：
   - 通过 GitHub API 搜索 `topic:dsh-plugin` 的仓库
   - 支持关键词搜索扩展
   - 结果缓存（5分钟 TTL）

2. **插件安装流程**：
   ```
   用户选择插件 → 解析来源类型 → 获取插件信息 → 执行安装命令 → 注册本地记录
   ```
   - GitHub 来源：尝试通过 npm 包名安装，否则通过 git URL
   - npm 来源：直接通过 `dsh plugin --profile add` 安装

3. **本地注册表**：
   - 存储位置：`~/.dsh/manager/plugins.json`
   - 记录字段：id, name, version, source, type, profile, installedAt, enabled

### 3.3 CLI 命令行工具 (`@dsh-manager/cli`)

**技术栈**：Commander.js + Chalk + Inquirer + Ora

**设计原则**：
- 命令式 CLI 与交互式 TUI 并存
- 复杂的多步骤操作使用 TUI
- 简单的单步操作使用命令式
- 输出格式支持 `--json` 便于脚本集成

### 3.4 官网 (Website)

**设计**：纯静态站点，零后端依赖

**部署**：GitHub Pages（通过 GitHub Actions 自动部署）

**功能**：
- 产品介绍（Hero + Features）
- 快速安装引导
- 插件市场实时预览（调用 GitHub API）
- 文档入口
- 版本下载链接

## 四、插件市场数据流

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐
│ 用户请求  │───▶│ PluginRegistry │───▶│  GitHubAPI   │
│ 搜索插件  │    │  .search()   │    │  .searchRepo │
└──────────┘    └──────┬───────┘    └──────┬───────┘
                       │                   │
                       ▼                   ▼
                  ┌──────────┐    ┌──────────────┐
                  │ 本地缓存  │    │  GitHub API  │
                  │ (5min)   │    │  响应        │
                  └──────────┘    └──────┬───────┘
                                         │
                                         ▼
                                    ┌──────────┐
                                    │ 格式化数据 │
                                    └──────┬───┘
                                           │
              ┌─────────────────────────────┼──────────────┐
              │                             │              │
              ▼                             ▼              ▼
       ┌──────────┐                  ┌──────────┐   ┌──────────┐
       │  CLI 显示 │                  │  TUI 显示 │   │ Web 显示 │
       └──────────┘                  └──────────┘   └──────────┘
```

## 五、安装工作流

```
用户执行 dshm install dsh
        │
        ▼
┌─────────────────┐
│ 检测 Node.js 环境 │
│ 版本 >= 18       │
└────────┬────────┘
         │ 失败
         ▼
    ┌──────────┐
    │ 提示安装  │
    │ Node.js   │
    └──────────┘
         │ 成功
         ▼
┌─────────────────┐
│ 检测 npm 环境    │
└────────┬────────┘
         │ 失败
         ▼
    ┌──────────┐
    │ 提示安装  │
    │ npm      │
    └──────────┘
         │ 成功
         ▼
┌─────────────────┐
│ 创建 DSH_HOME    │
│ 目录结构         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 执行 npm install │
│ -g @deepseek-ai/ │
│ dsh             │
└────────┬────────┘
         │ 失败                │ 成功
         ▼                     ▼
    ┌──────────┐        ┌─────────────────┐
    │ 错误处理  │        │ 验证安装 & 记录  │
    │ 提供建议  │        │ 版本信息         │
    └──────────┘        └────────┬────────┘
                                │
                                ▼
                        ┌─────────────────┐
                        │ 显示成功信息 &    │
                        │ 下一步指南        │
                        └─────────────────┘
```

## 六、关键技术决策

### 6.1 为什么选择 Monorepo？

- 代码共享：core 和 marketplace 可以被 CLI 和未来可能的 GUI 复用
- 统一管理：版本、依赖、构建可以统一处理
- 独立发布：每个包可以独立版本化和发布

### 6.2 为什么选择 Commander.js？

- 与 DSH 官方 CLI 一致的技术栈
- 成熟的命令解析能力
- 支持子命令、自动帮助生成

### 6.3 为什么 TUI 使用 Inquirer 而非 Ink？

- Ink 需要 React 运行时，增加启动时间
- Inquirer 更轻量，适合 CLI 工具
- 提供足够丰富的交互组件

### 6.4 插件市场为什么不走 npm registry 搜索？

- npm registry 搜索不包含 `dsh-plugin` 标签
- GitHub 的 `topic` 机制更适合插件分类
- GitHub 提供更丰富的仓库信息（stars, README 等）

## 七、未来规划

### Phase 1 (当前) - CLI + TUI + Website
- ✅ 基础 CLI 框架
- ✅ 交互式 TUI
- ✅ 插件市场发现
- ✅ 安装管理
- ✅ 系统诊断
- ✅ 官网

### Phase 2 - 增强功能
- [ ] 插件依赖管理
- [ ] 插件版本回滚
- [ ] 批量安装/卸载
- [ ] 更丰富的配置可视化
- [ ] 多语言支持

### Phase 3 - 生态扩展
- [ ] VS Code 扩展
- [ ] 插件开发脚手架
- [ ] 插件性能分析
- [ ] 社区插件评分系统
- [ ] 企业级部署管理

## 八、相关资源

- [DeepSeek Harness GitHub](https://github.com/deepseek-ai/deepseek-harness)
- [DSH 插件生态](https://github.com/topics/dsh-plugin)
- [Cordis 框架](https://github.com/cordiverse/cordis)

---

> 本文档会随着项目发展持续更新。